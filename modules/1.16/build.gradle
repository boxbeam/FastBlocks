plugins {
    id 'java'
}
sourceCompatibility = 1.8
targetCompatibility = 16
dependencies {
    compileOnly files(projectDir.toPath().parent.parent.resolve("alljars/spigot-1.16.5.jar"))
    implementation project(":common")
}
sourceSets {
    main {
        java {
            srcDir "src"
        }
        resources {
            srcDir "res"
        }
    }
}